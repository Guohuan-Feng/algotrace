import{ArrayTraceVisualizer}from"../../shared/components/ArrayTraceVisualizer";import type{VisualizerProps}from"../../shared/types";import{codeLines,examples,title,type LinkedListDesignInput}from"./data";import{createLinkedListDesignDryRun,type LinkedListDesignFrame}from"./dryRun";export default function LinkedListDesignVisualizer(p:VisualizerProps){return <ArrayTraceVisualizer<LinkedListDesignInput,LinkedListDesignFrame>{...p}title={title}examples={examples}codeLines={codeLines}heading="Linked-list operation trace" inputLabel="input JSON" errorMessage='Use {"operations":["addAtHead 1","get 0"]}.' parseInput={v=>{const x=v as Partial<LinkedListDesignInput>;return Array.isArray(x?.operations)&&x.operations.every(o=>typeof o==="string"&&/^(get|deleteAtIndex|addAtHead|addAtTail) -?\d+$|^addAtIndex -?\d+ -?\d+$/.test(o))?{operations:x.operations}:null;}}createRun={x=>createLinkedListDesignDryRun(x.operations)}renderState={f=><div className="state-block"><h3>linked-list state</h3><div className="token-list"><span>head to tail: {f.list.join(" -> ")||"None"}</span><span>outputs = [{f.outputs.join(", ")}]</span></div></div>}/>;}
