import type{ArrayTraceFrame}from"../../shared/components/ArrayTraceVisualizer";export type LinkedListDesignFrame=ArrayTraceFrame&{phase:"start"|"insert"|"get"|"delete"|"done";list:number[];outputs:number[]};export function createLinkedListDesignDryRun(operations:string[]):{frames:LinkedListDesignFrame[]}{const list:number[]=[],outputs:number[]=[],frames:LinkedListDesignFrame[]=[];let index=-1;const push=(f:Omit<LinkedListDesignFrame,"cells"|"activeIndices"|"completeIndices"|"list"|"outputs">)=>frames.push({...f,cells:operations,activeIndices:index<0?[]:[index],completeIndices:Array.from({length:Math.max(index,0)},(_,i)=>i),list:[...list],outputs:[...outputs]});push({kind:"start",phase:"start",title:"Create an empty linked list",detail:"The visual shows the list's node order after each operation.",activeLines:[2,3],result:null});operations.forEach((op,i)=>{index=i;const[a,b,c]=op.split(" "),at=Number(b),value=Number(c);if(a==="addAtHead"){list.unshift(at);push({kind:"build",phase:"insert",title:"addAtHead("+at+")",detail:"Insert before the current head.",activeLines:[6,7],result:null});}else if(a==="addAtTail"){list.push(at);push({kind:"build",phase:"insert",title:"addAtTail("+at+")",detail:"Append after the current tail.",activeLines:[8,9],result:null});}else if(a==="addAtIndex"){if(at>=0&&at<=list.length)list.splice(at,0,value);push({kind:"build",phase:"insert",title:"addAtIndex("+at+", "+value+")",detail:"Insert only when the index is within 0 through current length.",activeLines:[10,11],result:null});}else if(a==="deleteAtIndex"){if(at>=0&&at<list.length)list.splice(at,1);push({kind:"backtrack",phase:"delete",title:"deleteAtIndex("+at+")",detail:"Reconnect the neighboring logical nodes after deletion.",activeLines:[12,13],result:null});}else{const out=at>=0&&at<list.length?list[at]!:-1;outputs.push(out);push({kind:"found",phase:"get",title:"get("+at+") -> "+out,detail:"Return -1 if the index is outside the list.",activeLines:[4,5],result:null});}});index=-1;push({kind:"done",phase:"done",title:"Operations complete",detail:"The list and get outputs match the replayed calls.",activeLines:[],result:[...outputs]});return{frames};}
