import{ArrayTraceVisualizer}from"../../shared/components/ArrayTraceVisualizer";import type{VisualizerProps}from"../../shared/types";import{codeLines,examples,title,type TimeMapInput}from"./data";import{createTimeMapDryRun,type TimeMapFrame}from"./dryRun";export default function TimeMapVisualizer(p:VisualizerProps){return <ArrayTraceVisualizer<TimeMapInput,TimeMapFrame>{...p}title={title}examples={examples}codeLines={codeLines}heading="Timestamp history binary search" inputLabel="input JSON" errorMessage='Use {"operations":["set foo bar 1","get foo 1"]}.' parseInput={v=>{const x=v as Partial<TimeMapInput>;return Array.isArray(x?.operations)&&x.operations.every(o=>typeof o==="string"&&/^(set \S+ \S+ \d+|get \S+ \d+)$/.test(o))?{operations:x.operations}:null;}}createRun={x=>createTimeMapDryRun(x.operations)}renderState={f=><div className="state-block"><h3>key history</h3><div className="token-list"><span>history = {JSON.stringify(f.history)}</span><span>outputs = {JSON.stringify(f.outputs)}</span></div></div>}/>;}
