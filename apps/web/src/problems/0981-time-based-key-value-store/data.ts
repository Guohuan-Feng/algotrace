export type TimeMapInput={operations:string[]};export type TimeMapExample={id:number;label:string;input:TimeMapInput;output:Array<string|null>};export const title="981. Time Based Key-Value Store";export const examples:TimeMapExample[]=[{id:1,label:"LeetCode 1",input:{operations:["set foo bar 1","get foo 1","get foo 3","set foo bar2 4","get foo 4","get foo 5"]},output:[null,"bar","bar",null,"bar2","bar2"]}];export const defaultExample=examples[0]!;export const codeLines=["class TimeMap:","    def __init__(self):","        self.data = defaultdict(list)","    def set(self, key, value, timestamp):","        self.data[key].append((timestamp, value))","    def get(self, key, timestamp):","        history = self.data[key]","        left, right, answer = 0, len(history) - 1, ''","        while left <= right:","            mid = left + (right - left) // 2","            if history[mid][0] <= timestamp:","                answer = history[mid][1]","                left = mid + 1","            else:","                right = mid - 1","        return answer"];
