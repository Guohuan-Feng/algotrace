import type{ArrayTraceFrame}from"../../shared/components/ArrayTraceVisualizer";export type HashSetFrame=ArrayTraceFrame&{phase:"start"|"add"|"remove"|"contains"|"done";operationIndex:number|null;set:number[];outputs:boolean[]};export function createHashSetDryRun(operations:string[]):{frames:HashSetFrame[]}{const data=new Set<number>(),outputs:boolean[]=[],frames:HashSetFrame[]=[];let operationIndex:number|null=null;const push=(f:Omit<HashSetFrame,"cells"|"activeIndices"|"completeIndices"|"operationIndex"|"set"|"outputs">)=>frames.push({...f,cells:operations,activeIndices:operationIndex===null?[]:[operationIndex],completeIndices:operationIndex===null?[]:Array.from({length:operationIndex},(_,i)=>i),operationIndex,set:[...data].sort((a,b)=>a-b),outputs:[...outputs]});push({kind:"start",phase:"start",title:"Create an empty set",detail:"Each key is stored at most once.",activeLines:[2,3],result:null});operations.forEach((operation,index)=>{operationIndex=index;const[method,raw]=operation.split(" ");const key=Number(raw);if(method==="add"){data.add(key);push({kind:"build",phase:"add",title:"add("+key+")",detail:"Insert the key if absent.",activeLines:[4,5],result:null});}else if(method==="remove"){data.delete(key);push({kind:"backtrack",phase:"remove",title:"remove("+key+")",detail:"Remove the key if present.",activeLines:[6,7],result:null});}else{const value=data.has(key);outputs.push(value);push({kind:"found",phase:"contains",title:"contains("+key+") -> "+value,detail:"Membership lookup reads the current set.",activeLines:[8,9],result:null});}});operationIndex=null;push({kind:"done",phase:"done",title:"Operations complete",detail:"All membership results are recorded.",activeLines:[],result:[...outputs]});return{frames};}
