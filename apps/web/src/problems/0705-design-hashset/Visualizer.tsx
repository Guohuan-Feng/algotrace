import{ArrayTraceVisualizer}from"../../shared/components/ArrayTraceVisualizer";import type{VisualizerProps}from"../../shared/types";import{codeLines,examples,title,type HashSetInput}from"./data";import{createHashSetDryRun,type HashSetFrame}from"./dryRun";export default function HashSetVisualizer(p:VisualizerProps){return <ArrayTraceVisualizer<HashSetInput,HashSetFrame>{...p}title={title}examples={examples}codeLines={codeLines}heading="HashSet operation trace" inputLabel="input JSON" errorMessage='Use {"operations":["add 1","contains 1"]}.' parseInput={v=>{const x=v as Partial<HashSetInput>;return Array.isArray(x?.operations)&&x.operations.every(o=>typeof o==="string"&&/^(add|remove|contains) -?\d+$/.test(o))?{operations:x.operations}:null;}}createRun={x=>createHashSetDryRun(x.operations)}renderState={f=><div className="state-block"><h3>set state</h3><div className="token-list"><span>set = [{f.set.join(", ")}]</span><span>outputs = [{f.outputs.join(", ")}]</span></div></div>}/>;}
