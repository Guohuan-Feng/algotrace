import type { FrameKind } from "../../shared/types";
export type GuessNumberFrame = { kind: FrameKind; phase: "start" | "guess" | "higher" | "lower" | "found" | "done"; title: string; detail: string; activeLines: number[]; left: number; right: number; mid: number | null; response: -1 | 0 | 1 | null; result: number | null };
export function createGuessNumberDryRun(n: number, pick: number): { frames: GuessNumberFrame[] } { const frames: GuessNumberFrame[] = []; let left = 1; let right = n; const push = (frame: Omit<GuessNumberFrame, "left" | "right">) => frames.push({ ...frame, left, right }); push({ kind: "start", phase: "start", title: "Search numbers 1 through n", detail: `The hidden pick is somewhere in the inclusive range [1, ${n}].`, activeLines: [4], mid: null, response: null, result: null }); while (left <= right) { const mid = left + Math.floor((right - left) / 2); const response: -1 | 0 | 1 = mid === pick ? 0 : mid < pick ? 1 : -1; push({ kind: "visit", phase: "guess", title: `guess(${mid}) returns ${response}`, detail: response === 0 ? "The API reports an exact match." : response === 1 ? "The API says the hidden number is higher." : "The API says the hidden number is lower.", activeLines: [6, 7, 8], mid, response, result: null }); if (response === 0) { push({ kind: "found", phase: "found", title: `Return ${mid}`, detail: "A response of 0 identifies the hidden number.", activeLines: [9, 10], mid, response, result: mid }); return { frames }; } if (response === 1) { left = mid + 1; push({ kind: "build", phase: "higher", title: `Move left to ${left}`, detail: `All numbers through ${mid} are too small, so keep only the higher half.`, activeLines: [11, 12], mid, response, result: null }); } else { right = mid - 1; push({ kind: "prune", phase: "lower", title: `Move right to ${right}`, detail: `All numbers from ${mid} upward are too large, so keep only the lower half.`, activeLines: [13, 14], mid, response, result: null }); } } push({ kind: "done", phase: "done", title: "No number found", detail: "The search range is empty.", activeLines: [16], mid: null, response: null, result: -1 }); return { frames }; }
