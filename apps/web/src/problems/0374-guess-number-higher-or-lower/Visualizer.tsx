import { useEffect, useMemo, useState } from "react";
import { ArrowLeft, Upload } from "lucide-react";
import { CodeTrace } from "../../shared/components/CodeTrace";
import { StepControls } from "../../shared/components/StepControls";
import type { VisualizerProps } from "../../shared/types";
import { codeLines, defaultExample, examples, title, type GuessNumberExample, type GuessNumberInput } from "./data";
import { createGuessNumberDryRun } from "./dryRun";
export default function GuessNumberVisualizer({ onBack }: VisualizerProps) { const [exampleId, setExampleId] = useState(defaultExample.id), [input, setInput] = useState<GuessNumberInput>(defaultExample.input), [text, setText] = useState(JSON.stringify(defaultExample.input)), [step, setStep] = useState(0), [playing, setPlaying] = useState(false), [error, setError] = useState(""); const run = useMemo(() => createGuessNumberDryRun(input.n, input.pick), [input]); const frame = run.frames[Math.min(step, run.frames.length - 1)]!; const selected = examples.find((example) => example.id === exampleId); const values = Array.from({ length: input.n }, (_, index) => index + 1); useEffect(() => { if (!playing || step >= run.frames.length - 1) { if (step >= run.frames.length - 1) setPlaying(false); return; } const timer = window.setTimeout(() => setStep((current) => current + 1), 650); return () => window.clearTimeout(timer); }, [playing, run.frames.length, step]); function load(example: GuessNumberExample) { setExampleId(example.id); setInput(example.input); setText(JSON.stringify(example.input)); setStep(0); setPlaying(false); setError(""); } function loadInput() { try { const parsed: unknown = JSON.parse(text); if (!valid(parsed)) throw new Error(); setExampleId(0); setInput(parsed); setStep(0); setPlaying(false); setError(""); } catch { setError('Use {"n":10,"pick":6}; n is at most 24 for the view.'); } } return <main className="app-shell"><header className="topbar"><div><button className="back-link compact" onClick={onBack} type="button"><ArrowLeft size={16} />Catalog</button><p className="eyebrow">AlgoTrace dry run</p><h1>{title}</h1></div><div className="step-pill">Step {step + 1} / {run.frames.length}</div></header><section className="workspace"><aside className="board-panel"><div className="example-switcher"><span>LeetCode examples</span><div>{examples.map((example) => <button className={example.id === exampleId ? "active" : ""} key={example.id} onClick={() => load(example)} type="button">{example.id}</button>)}</div></div><div className="input-grid"><label>input JSON<textarea aria-label="guess number input JSON" value={text} onChange={(event) => setText(event.target.value)} /></label>{error ? <p className="error">{error}</p> : null}<button className="command load" onClick={loadInput} type="button"><Upload size={16} />Load input</button></div><div className="expected-output"><span>{selected ? `${selected.label} output` : "Current result"}</span><code>{frame.result ?? "pending"}</code></div></aside><section className="flow-panel binary-flow-panel"><div className="panel-heading"><h2>Guess API binary search</h2><span>hidden pick = {input.pick}</span></div><div className="binary-stage"><div className="binary-array">{values.map((value) => <div className={["binary-cell", value >= frame.left && value <= frame.right ? "in-window" : "is-discarded", value === frame.left ? "is-left" : "", value === frame.mid ? "is-mid" : "", value === frame.right ? "is-right" : "", frame.result === value ? "is-found" : ""].filter(Boolean).join(" ")} key={value}><strong>{value}</strong><span>{value === frame.left ? "left" : value === frame.mid ? "mid" : value === frame.right ? "right" : ""}</span></div>)}</div><div className="insert-slot"><span>guess(mid)</span><strong>{frame.response === null ? "waiting" : frame.response}</strong></div></div></section><aside className="state-panel"><div className="state-sticky"><div className={`event-card ${frame.kind}`}><p className="eyebrow">{frame.phase}</p><h2>{frame.title}</h2><p>{frame.detail}</p></div><StepControls frameCount={run.frames.length} playing={playing} step={step} onPlayingChange={setPlaying} onStepChange={setStep} /></div><div className="state-block"><h3>boundaries</h3><div className="token-list"><span>left = {frame.left}</span><span>mid = {frame.mid ?? "-"}</span><span>right = {frame.right}</span><span>response = {frame.response ?? "-"}</span></div></div><CodeTrace activeLines={frame.activeLines} codeLines={codeLines} /></aside></section></main>; }
function valid(value: unknown): value is GuessNumberInput { if (typeof value !== "object" || value === null) return false; const candidate = value as { n?: unknown; pick?: unknown }; return typeof candidate.n === "number" && Number.isInteger(candidate.n) && candidate.n >= 1 && candidate.n <= 24 && typeof candidate.pick === "number" && Number.isInteger(candidate.pick) && candidate.pick >= 1 && candidate.pick <= candidate.n; }
