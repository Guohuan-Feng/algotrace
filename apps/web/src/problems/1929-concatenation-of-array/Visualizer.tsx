import {ArrayTraceVisualizer} from "../../shared/components/ArrayTraceVisualizer";import type{VisualizerProps}from"../../shared/types";import{codeLines,examples,title,type ConcatenationInput}from"./data";import{createConcatenationDryRun,type ConcatenationFrame}from"./dryRun";export default function ConcatenationVisualizer(props:VisualizerProps){return <ArrayTraceVisualizer<ConcatenationInput,ConcatenationFrame> {...props} title={title} examples={examples} codeLines={codeLines} heading="Write both halves" inputLabel="input JSON" errorMessage='Use {"nums":[1,2,1]}.' parseInput={v=>{const x=v as Partial<ConcatenationInput>;return Array.isArray(x?.nums)&&x.nums.every(Number.isFinite)?{nums:x.nums}:null;}} createRun={x=>createConcatenationDryRun(x.nums)} renderState={f=><div className="state-block"><h3>output array</h3><div className="token-list"><span>i = {f.index??"-"}</span><span>answer = [{f.output.join(", ")}]</span></div></div>}/>;}
