import{ArrayTraceVisualizer}from"../../shared/components/ArrayTraceVisualizer";import type{VisualizerProps}from"../../shared/types";import{codeLines,examples,title,type CommonCharactersInput}from"./data";import{createCommonCharactersDryRun,type CommonCharactersFrame}from"./dryRun";export default function CommonCharactersVisualizer(props:VisualizerProps){return <ArrayTraceVisualizer<CommonCharactersInput,CommonCharactersFrame>{...props}title={title}examples={examples}codeLines={codeLines}heading="Frequency-map intersection" inputLabel="input JSON" errorMessage='Use {"words":["bella","label","roller"]}.' parseInput={v=>{const x=v as Partial<CommonCharactersInput>;return Array.isArray(x?.words)&&x.words.length>0&&x.words.every(w=>typeof w==="string")?{words:x.words}:null;}}createRun={x=>createCommonCharactersDryRun(x.words)}renderState={f=><div className="state-block"><h3>common counts</h3><div className="token-list"><span>word index = {f.wordIndex}</span><span>{JSON.stringify(f.common)}</span></div></div>}/>;}
