import { ArrayTraceVisualizer } from "../../shared/components/ArrayTraceVisualizer"; import type { VisualizerProps } from "../../shared/types"; import { codeLines, examples, title, type PermutationInput } from "./data"; import { createPermutationInStringDryRun, type PermutationFrame } from "./dryRun";
export default function PermutationVisualizer(props: VisualizerProps) { return <ArrayTraceVisualizer<PermutationInput, PermutationFrame> {...props} title={title} examples={examples} codeLines={codeLines} heading="Fixed-size permutation window" inputLabel="input JSON" errorMessage='Use {"s1":"ab","s2":"eidbaooo"}.' parseInput={(value) => { const candidate = value as Partial<PermutationInput>; return typeof candidate?.s1 === "string" && typeof candidate.s2 === "string" && candidate.s1.length <= 12 && candidate.s2.length <= 32 ? { s1: candidate.s1, s2: candidate.s2 } : null; }} createRun={(input) => createPermutationInStringDryRun(input.s1, input.s2)} renderState={(frame) => <div className="state-block"><h3>window state</h3><div className="token-list"><span>left = {frame.left}</span><span>right = {frame.right}</span><span>window = {JSON.stringify(frame.window)}</span><span>need = {JSON.stringify(Object.fromEntries(frame.needEntries))}</span><span>window = {JSON.stringify(Object.fromEntries(frame.windowEntries))}</span></div></div>} />; }
