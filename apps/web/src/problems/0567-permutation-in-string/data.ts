export type PermutationInput = { s1: string; s2: string }; export type PermutationExample = { id: number; label: string; input: PermutationInput; output: boolean }; export const title = "567. Permutation in String"; export const examples: PermutationExample[] = [{ id: 1, label: "LeetCode 1", input: { s1: "ab", s2: "eidbaooo" }, output: true }, { id: 2, label: "LeetCode 2", input: { s1: "ab", s2: "eidboaoo" }, output: false }]; export const defaultExample = examples[0]!;
export const codeLines = ["class Solution:", "    def checkInclusion(self, s1: str, s2: str) -> bool:", "        need = Counter(s1)", "        window = Counter()", "        left = 0", "        for right, char in enumerate(s2):", "            window[char] += 1", "            if right - left + 1 > len(s1):", "                window[s2[left]] -= 1", "                left += 1", "            if window == need:", "                return True", "        return False"];
