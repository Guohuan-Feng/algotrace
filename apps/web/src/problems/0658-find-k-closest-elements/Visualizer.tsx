import { ArrayTraceVisualizer } from "../../shared/components/ArrayTraceVisualizer"; import type { VisualizerProps } from "../../shared/types"; import { codeLines, examples, title, type KClosestInput } from "./data"; import { createKClosestDryRun, type KClosestFrame } from "./dryRun";
export default function KClosestVisualizer(props: VisualizerProps) { return <ArrayTraceVisualizer<KClosestInput, KClosestFrame> {...props} title={title} examples={examples} codeLines={codeLines} heading="Shrink to the closest window" inputLabel="input JSON" errorMessage='Use {"arr":[1,2,3,4,5],"k":4,"x":3}.' parseInput={(value) => { const candidate = value as Partial<KClosestInput>; return Array.isArray(candidate?.arr) && candidate.arr.every(Number.isFinite) && typeof candidate.k === "number" && Number.isInteger(candidate.k) && candidate.k > 0 && candidate.k <= candidate.arr.length && typeof candidate.x === "number" ? { arr: candidate.arr, k: candidate.k, x: candidate.x } : null; }} createRun={(input) => createKClosestDryRun(input.arr, input.k, input.x)} renderState={(frame) => <div className="state-block"><h3>endpoint distances</h3><div className="token-list"><span>left = {frame.left}</span><span>right = {frame.right}</span><span>x - arr[left] = {frame.leftDistance ?? "-"}</span><span>arr[right] - x = {frame.rightDistance ?? "-"}</span></div></div>} />; }
