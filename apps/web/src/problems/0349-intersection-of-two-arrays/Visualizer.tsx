import { useEffect, useMemo, useState } from "react";
import { ArrowLeft, Upload } from "lucide-react";
import { CodeTrace } from "../../shared/components/CodeTrace";
import { StepControls } from "../../shared/components/StepControls";
import type { VisualizerProps } from "../../shared/types";
import { codeLines, defaultExample, examples, title, type IntersectionExample, type IntersectionInput } from "./data";
import { createIntersectionDryRun } from "./dryRun";
export default function IntersectionVisualizer({ onBack }: VisualizerProps) { const [exampleId, setExampleId] = useState(defaultExample.id), [input, setInput] = useState<IntersectionInput>(defaultExample.input), [text, setText] = useState(JSON.stringify(defaultExample.input)), [step, setStep] = useState(0), [playing, setPlaying] = useState(false), [error, setError] = useState(""); const run = useMemo(() => createIntersectionDryRun(input.nums1, input.nums2), [input]); const frame = run.frames[Math.min(step, run.frames.length - 1)]!; const selected = examples.find((example) => example.id === exampleId); useEffect(() => { if (!playing || step >= run.frames.length - 1) { if (step >= run.frames.length - 1) setPlaying(false); return; } const timer = window.setTimeout(() => setStep((current) => current + 1), 650); return () => window.clearTimeout(timer); }, [playing, run.frames.length, step]); function load(example: IntersectionExample) { setExampleId(example.id); setInput(example.input); setText(JSON.stringify(example.input)); setStep(0); setPlaying(false); setError(""); } function loadInput() { try { const parsed: unknown = JSON.parse(text); if (!valid(parsed)) throw new Error(); setExampleId(0); setInput(parsed); setStep(0); setPlaying(false); setError(""); } catch { setError('Use {"nums1":[1,2,2,1],"nums2":[2,2]}.'); } } return <main className="app-shell"><header className="topbar"><div><button className="back-link compact" onClick={onBack} type="button"><ArrowLeft size={16} />Catalog</button><p className="eyebrow">AlgoTrace dry run</p><h1>{title}</h1></div><div className="step-pill">Step {step + 1} / {run.frames.length}</div></header><section className="workspace"><aside className="board-panel"><div className="example-switcher"><span>LeetCode examples</span><div>{examples.map((example) => <button className={example.id === exampleId ? "active" : ""} key={example.id} onClick={() => load(example)} type="button">{example.id}</button>)}</div></div><div className="input-grid"><label>input JSON<textarea aria-label="intersection input JSON" value={text} onChange={(event) => setText(event.target.value)} /></label>{error ? <p className="error">{error}</p> : null}<button className="command load" onClick={loadInput} type="button"><Upload size={16} />Load input</button></div><div className="expected-output"><span>{selected ? `${selected.label} output` : "Current result"}</span><code>{frame.result ? JSON.stringify(frame.result) : "pending"}</code></div></aside><section className="flow-panel product-flow-panel"><div className="panel-heading"><h2>Set lookup for unique intersection</h2><span>yellow: nums2 scan, green: intersection</span></div><div className="product-stage"><div className="product-state-grid"><div className="product-state is-emphasized"><span>seen = set(nums1)</span><strong>{frame.seen.length ? `{${frame.seen.join(", ")}}` : "{}"}</strong><small>from nums1 = [{input.nums1.join(", ")}]</small></div><div className="product-state is-result"><span>result set</span><strong>{frame.resultSet.length ? `{${frame.resultSet.join(", ")}}` : "{}"}</strong></div></div><div className="product-numbers">{input.nums2.map((value, index) => <div className={["product-number", index === frame.index ? "is-current" : "", frame.resultSet.includes(value) ? "is-window" : ""].filter(Boolean).join(" ")} key={index}><span>nums2[{index}]</span><strong>{value}</strong><em>{index === frame.index ? "current" : ""}</em></div>)}</div><div className="product-phase"><span>membership lookup</span><strong>current = {frame.current ?? "-"}</strong><p>{frame.detail}</p></div></div></section><aside className="state-panel"><div className="state-sticky"><div className={`event-card ${frame.kind}`}><p className="eyebrow">{frame.phase}</p><h2>{frame.title}</h2><p>{frame.detail}</p></div><StepControls frameCount={run.frames.length} playing={playing} step={step} onPlayingChange={setPlaying} onStepChange={setStep} /></div><div className="state-block"><h3>set state</h3><div className="token-list"><span>seen size = {frame.seen.length}</span><span>result size = {frame.resultSet.length}</span></div></div><CodeTrace activeLines={frame.activeLines} codeLines={codeLines} /></aside></section></main>; }
function valid(value: unknown): value is IntersectionInput { if (typeof value !== "object" || value === null) return false; const candidate = value as { nums1?: unknown; nums2?: unknown }; return [candidate.nums1, candidate.nums2].every((nums) => Array.isArray(nums) && nums.length <= 16 && nums.every((item) => typeof item === "number" && Number.isInteger(item))); }
