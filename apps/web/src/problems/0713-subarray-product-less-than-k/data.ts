export type SubarrayProductInput = { nums: number[]; k: number }; export type SubarrayProductExample = { id: number; label: string; input: SubarrayProductInput; output: number }; export const title = "713. Subarray Product Less Than K"; export const examples: SubarrayProductExample[] = [{ id: 1, label: "LeetCode 1", input: { nums: [10, 5, 2, 6], k: 100 }, output: 8 }, { id: 2, label: "LeetCode 2", input: { nums: [1, 2, 3], k: 0 }, output: 0 }]; export const defaultExample = examples[0]!;
export const codeLines = ["class Solution:", "    def numSubarrayProductLessThanK(self, nums: List[int], k: int) -> int:", "        if k <= 1:", "            return 0", "        product, answer = 1, 0", "        left = 0", "        for right, num in enumerate(nums):", "            product *= num", "            while product >= k:", "                product //= nums[left]", "                left += 1", "            answer += right - left + 1", "        return answer"];
