import { ArrayTraceVisualizer } from "../../shared/components/ArrayTraceVisualizer"; import type { VisualizerProps } from "../../shared/types"; import { codeLines, examples, title, type SubarrayProductInput } from "./data"; import { createSubarrayProductDryRun, type SubarrayProductFrame } from "./dryRun";
export default function SubarrayProductVisualizer(props: VisualizerProps) { return <ArrayTraceVisualizer<SubarrayProductInput, SubarrayProductFrame> {...props} title={title} examples={examples} codeLines={codeLines} heading="Variable-size product window" inputLabel="input JSON" errorMessage='Use {"nums":[10,5,2,6],"k":100}.' parseInput={(value) => { const candidate = value as Partial<SubarrayProductInput>; return Array.isArray(candidate?.nums) && candidate.nums.every((num) => Number.isFinite(num) && num > 0) && typeof candidate.k === "number" ? { nums: candidate.nums, k: candidate.k } : null; }} createRun={(input) => createSubarrayProductDryRun(input.nums, input.k)} renderState={(frame) => <div className="state-block"><h3>window product</h3><div className="token-list"><span>left = {frame.left}</span><span>right = {frame.right}</span><span>product = {frame.product}</span><span>count = {frame.count}</span></div></div>} />; }
