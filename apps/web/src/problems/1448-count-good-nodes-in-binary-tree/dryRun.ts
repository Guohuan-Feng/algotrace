import type{ArrayTraceFrame}from"../../shared/components/ArrayTraceVisualizer";export type GoodNodesFrame=ArrayTraceFrame&{phase:"start"|"visit"|"good"|"done";pathMax:number;count:number;tree:Array<number|null>};export function createGoodNodesDryRun(values:Array<number|null>):{frames:GoodNodesFrame[]}{const frames:GoodNodesFrame[]=[];let index=0,pathMax=typeof values[0]==="number"?values[0]!:0,count=0;const done=new Set<number>();const push=(f:Omit<GoodNodesFrame,"cells"|"activeIndices"|"completeIndices"|"pathMax"|"count"|"tree">)=>frames.push({...f,cells:values.map(v=>v??"∅"),activeIndices:typeof values[index]==="number"?[index]:[],completeIndices:[...done],pathMax,count,tree:[...values]});push({kind:"start",phase:"start",title:"Start DFS with root maximum",detail:"Each call receives the largest value on its path.",activeLines:[2,9],result:null});dfs(0,values[0]??0);push({kind:"done",phase:"done",title:"Return "+count,detail:"Every node has compared itself with its ancestor maximum.",activeLines:[9],result:count});return{frames};function dfs(i:number,max:number){if(typeof values[i]!=="number")return;index=i;pathMax=max;const value=values[i]!;push({kind:"visit",phase:"visit",title:"Visit "+value,detail:"Path maximum is "+max+".",activeLines:[3,4,5],result:null});const good=value>=max;if(good)count+=1;pathMax=Math.max(max,value);done.add(i);push({kind:good?"found":"visit",phase:"good",title:good?value+" is good":value+" is not good",detail:"A good node is at least its path maximum.",activeLines:[6,7],result:null});dfs(i*2+1,pathMax);dfs(i*2+2,pathMax);}}
