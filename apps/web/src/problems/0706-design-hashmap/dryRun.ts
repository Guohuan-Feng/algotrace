import type{ArrayTraceFrame}from"../../shared/components/ArrayTraceVisualizer";export type HashMapFrame=ArrayTraceFrame&{phase:"start"|"put"|"get"|"remove"|"done";entries:Array<[number,number]>;outputs:number[]};export function createHashMapDryRun(operations:string[]):{frames:HashMapFrame[]}{const map=new Map<number,number>(),outputs:number[]=[],frames:HashMapFrame[]=[];let index=-1;const push=(f:Omit<HashMapFrame,"cells"|"activeIndices"|"completeIndices"|"entries"|"outputs">)=>frames.push({...f,cells:operations,activeIndices:index<0?[]:[index],completeIndices:Array.from({length:Math.max(index,0)},(_,i)=>i),entries:[...map.entries()].sort((a,b)=>a[0]-b[0]),outputs:[...outputs]});push({kind:"start",phase:"start",title:"Create an empty map",detail:"Each key maps to at most one value.",activeLines:[2,3],result:null});operations.forEach((op,i)=>{index=i;const[a,b,c]=op.split(" "),key=Number(b);if(a==="put"){map.set(key,Number(c));push({kind:"build",phase:"put",title:"put("+key+", "+c+")",detail:"Insert or replace the key value.",activeLines:[4,5],result:null});}else if(a==="remove"){map.delete(key);push({kind:"backtrack",phase:"remove",title:"remove("+key+")",detail:"Delete this key if present.",activeLines:[8,9],result:null});}else{const value=map.get(key)??-1;outputs.push(value);push({kind:"found",phase:"get",title:"get("+key+") -> "+value,detail:"Missing keys return -1.",activeLines:[6,7],result:null});}});index=-1;push({kind:"done",phase:"done",title:"Operations complete",detail:"Every get result has been recorded.",activeLines:[],result:[...outputs]});return{frames};}
