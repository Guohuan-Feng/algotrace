import{ArrayTraceVisualizer}from"../../shared/components/ArrayTraceVisualizer";import type{VisualizerProps}from"../../shared/types";import{codeLines,examples,title,type HashMapInput}from"./data";import{createHashMapDryRun,type HashMapFrame}from"./dryRun";export default function HashMapVisualizer(p:VisualizerProps){return <ArrayTraceVisualizer<HashMapInput,HashMapFrame>{...p}title={title}examples={examples}codeLines={codeLines}heading="HashMap operation trace" inputLabel="input JSON" errorMessage='Use {"operations":["put 1 1","get 1"]}.' parseInput={v=>{const x=v as Partial<HashMapInput>;return Array.isArray(x?.operations)&&x.operations.every(o=>typeof o==="string"&&/^(put -?\d+ -?\d+|get -?\d+|remove -?\d+)$/.test(o))?{operations:x.operations}:null;}}createRun={x=>createHashMapDryRun(x.operations)}renderState={f=><div className="state-block"><h3>map state</h3><div className="token-list"><span>entries = {JSON.stringify(f.entries)}</span><span>outputs = [{f.outputs.join(", ")}]</span></div></div>}/>;}
