import { ArrayTraceVisualizer } from "../../shared/components/ArrayTraceVisualizer"; import type { VisualizerProps } from "../../shared/types"; import { codeLines, examples, title, type BinarySearchInput } from "./data"; import { createBinarySearchDryRun, type BinarySearchFrame } from "./dryRun";
export default function BinarySearchVisualizer(props: VisualizerProps) { return <ArrayTraceVisualizer<BinarySearchInput, BinarySearchFrame> {...props} title={title} examples={examples} codeLines={codeLines} heading="Closed-interval binary search" inputLabel="input JSON" errorMessage='Use {"nums":[-1,0,3,5,9,12],"target":9}.' parseInput={(value) => { const candidate = value as Partial<BinarySearchInput>; return Array.isArray(candidate?.nums) && candidate.nums.every(Number.isFinite) && candidate.nums.every((value, index, array) => index === 0 || array[index - 1]! < value) && typeof candidate.target === "number" ? { nums: candidate.nums, target: candidate.target } : null; }} createRun={(input) => createBinarySearchDryRun(input.nums, input.target)} renderState={(frame) => <div className="state-block"><h3>binary-search bounds</h3><div className="token-list"><span>left = {frame.left}</span><span>right = {frame.right}</span><span>mid = {frame.mid ?? "-"}</span><span>target = {frame.target}</span></div></div>} />; }
