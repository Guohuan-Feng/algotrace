export type BinarySearchInput = { nums: number[]; target: number }; export type BinarySearchExample = { id: number; label: string; input: BinarySearchInput; output: number }; export const title = "704. Binary Search"; export const examples: BinarySearchExample[] = [{ id: 1, label: "LeetCode 1", input: { nums: [-1, 0, 3, 5, 9, 12], target: 9 }, output: 4 }, { id: 2, label: "LeetCode 2", input: { nums: [-1, 0, 3, 5, 9, 12], target: 2 }, output: -1 }]; export const defaultExample = examples[0]!;
export const codeLines = ["class Solution:", "    def search(self, nums: List[int], target: int) -> int:", "        left, right = 0, len(nums) - 1", "        while left <= right:", "            mid = left + (right - left) // 2", "            if nums[mid] == target:", "                return mid", "            if nums[mid] < target:", "                left = mid + 1", "            else:", "                right = mid - 1", "        return -1"];
