import{ArrayTraceVisualizer}from"../../shared/components/ArrayTraceVisualizer";import type{VisualizerProps}from"../../shared/types";import{codeLines,examples,title,type MiddleNodeInput}from"./data";import{createMiddleNodeDryRun,type MiddleNodeFrame}from"./dryRun";export default function MiddleNodeVisualizer(p:VisualizerProps){return <ArrayTraceVisualizer<MiddleNodeInput,MiddleNodeFrame>{...p}title={title}examples={examples}codeLines={codeLines}heading="Fast and slow pointer trace" inputLabel="input JSON" errorMessage='Use {"values":[1,2,3,4,5]}.' parseInput={v=>{const x=v as Partial<MiddleNodeInput>;return Array.isArray(x?.values)&&x.values.every(Number.isFinite)&&x.values.length>0?{values:x.values}:null;}}createRun={x=>createMiddleNodeDryRun(x.values)}renderState={f=><div className="state-block"><h3>pointers</h3><div className="token-list"><span>slow = {f.slow??"past tail"}</span><span>fast = {f.fast??"past tail"}</span></div></div>}/>;}
