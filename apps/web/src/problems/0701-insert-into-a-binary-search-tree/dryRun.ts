import type{ArrayTraceFrame}from"../../shared/components/ArrayTraceVisualizer";export type InsertBstFrame=ArrayTraceFrame&{phase:"start"|"compare"|"move"|"insert"|"done";current:number|null;val:number;tree:Array<number|null>};export function createInsertBstDryRun(input:Array<number|null>,val:number):{frames:InsertBstFrame[]}{const tree=[...input],frames:InsertBstFrame[]=[];let index=0;const push=(f:Omit<InsertBstFrame,"cells"|"activeIndices"|"completeIndices"|"current"|"val"|"tree">)=>frames.push({...f,cells:tree.map(v=>v??"∅"),activeIndices:index<tree.length?[index]:[],completeIndices:[],current:typeof tree[index]==="number"?tree[index]!:null,val,tree:[...tree]});if(typeof tree[0]!=="number"){tree[0]=val;push({kind:"found",phase:"insert",title:"Insert as root",detail:"The empty tree receives the new root.",activeLines:[2,3,4],result:[...tree]});return{frames};}push({kind:"start",phase:"start",title:"Start at root",detail:"Follow BST ordering until a child slot is empty.",activeLines:[5,6],result:null});while(true){const cur=tree[index]!;push({kind:"visit",phase:"compare",title:"Compare "+val+" with "+cur,detail:"Choose left for smaller values and right otherwise.",activeLines:[6,7,12],result:null});const next=val<cur?index*2+1:index*2+2;if(typeof tree[next]!=="number"){while(tree.length<=next)tree.push(null);tree[next]=val;index=next;push({kind:"found",phase:"insert",title:"Insert "+val+" at empty child",detail:"This slot preserves the BST ordering.",activeLines:val<cur?[8,9,10]:[13,14,15],result:null});break;}index=next;push({kind:"visit",phase:"move",title:"Move to existing child",detail:"Continue down the only ordered branch.",activeLines:val<cur?[11]:[16],result:null});}push({kind:"done",phase:"done",title:"Return root",detail:"The modified tree contains the new value.",activeLines:[17],result:[...tree]});return{frames};}
