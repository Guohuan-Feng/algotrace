import{ArrayTraceVisualizer}from"../../shared/components/ArrayTraceVisualizer";import type{VisualizerProps}from"../../shared/types";import{codeLines,examples,title,type SortArrayInput}from"./data";import{createSortArrayDryRun,type SortArrayFrame}from"./dryRun";export default function SortArrayVisualizer(p:VisualizerProps){return <ArrayTraceVisualizer<SortArrayInput,SortArrayFrame>{...p}title={title}examples={examples}codeLines={codeLines}heading="Quick-sort partition trace" inputLabel="input JSON" errorMessage='Use {"nums":[5,2,3,1]}.' parseInput={v=>{const x=v as Partial<SortArrayInput>;return Array.isArray(x?.nums)&&x.nums.every(Number.isFinite)?{nums:x.nums}:null;}}createRun={x=>createSortArrayDryRun(x.nums)}renderState={f=><div className="state-block"><h3>partition state</h3><div className="token-list"><span>left = {f.left}</span><span>right = {f.right}</span><span>pivot = {f.pivot??"-"}</span></div></div>}/>;}
