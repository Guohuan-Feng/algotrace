import { ArrayTraceVisualizer } from "../../shared/components/ArrayTraceVisualizer"; import type { VisualizerProps } from "../../shared/types"; import { codeLines, examples, title, type RescueBoatsInput } from "./data"; import { createRescueBoatsDryRun, type RescueBoatsFrame } from "./dryRun";
export default function RescueBoatsVisualizer(props: VisualizerProps) { return <ArrayTraceVisualizer<RescueBoatsInput, RescueBoatsFrame> {...props} title={title} examples={examples} codeLines={codeLines} heading="Greedy passenger pairing" inputLabel="input JSON" errorMessage='Use {"people":[3,2,2,1],"limit":3}.' parseInput={(value) => { const candidate = value as Partial<RescueBoatsInput>; return Array.isArray(candidate?.people) && candidate.people.every((person) => Number.isFinite(person) && person > 0) && typeof candidate.limit === "number" ? { people: candidate.people, limit: candidate.limit } : null; }} createRun={(input) => createRescueBoatsDryRun(input.people, input.limit)} renderState={(frame) => <div className="state-block"><h3>boat assignment</h3><div className="token-list"><span>left = {frame.left}</span><span>right = {frame.right}</span><span>limit = {frame.limit}</span><span>pair fits = {String(frame.paired)}</span><span>boats = {frame.boats}</span></div></div>} />; }
