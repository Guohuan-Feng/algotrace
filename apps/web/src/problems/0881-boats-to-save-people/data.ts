export type RescueBoatsInput = { people: number[]; limit: number }; export type RescueBoatsExample = { id: number; label: string; input: RescueBoatsInput; output: number }; export const title = "881. Boats to Save People"; export const examples: RescueBoatsExample[] = [{ id: 1, label: "LeetCode 1", input: { people: [1, 2], limit: 3 }, output: 1 }, { id: 2, label: "LeetCode 2", input: { people: [3, 2, 2, 1], limit: 3 }, output: 3 }]; export const defaultExample = examples[0]!;
export const codeLines = ["class Solution:", "    def numRescueBoats(self, people: List[int], limit: int) -> int:", "        people.sort()", "        left, right = 0, len(people) - 1", "        boats = 0", "        while left <= right:", "            if people[left] + people[right] <= limit:", "                left += 1", "            right -= 1", "            boats += 1", "        return boats"];
