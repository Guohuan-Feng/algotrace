import type { ArrayTraceFrame } from "../../shared/components/ArrayTraceVisualizer";
export type RescueBoatsFrame = ArrayTraceFrame & { phase: "sort" | "pair" | "boat" | "done"; left: number; right: number; boats: number; limit: number; paired: boolean | null };
export function createRescueBoatsDryRun(people: number[], limit: number): { frames: RescueBoatsFrame[] } { const sorted = [...people].sort((a, b) => a - b); const frames: RescueBoatsFrame[] = []; let left = 0, right = sorted.length - 1, boats = 0, paired: boolean | null = null; const push = (frame: Omit<RescueBoatsFrame, "cells" | "activeIndices" | "completeIndices" | "left" | "right" | "boats" | "limit" | "paired">) => frames.push({ ...frame, cells: sorted, activeIndices: left <= right ? [left, right] : [], completeIndices: sorted.map((_, index) => index).filter((index) => index < left || index > right), left, right, boats, limit, paired }); push({ kind: "build", phase: "sort", title: "Sort passengers by weight", detail: "The lightest and heaviest passenger decide whether a pair can share a boat.", activeLines: [3, 4, 5], result: null }); while (left <= right) { paired = sorted[left]! + sorted[right]! <= limit; push({ kind: "visit", phase: "pair", title: `Check ${sorted[left]} + ${sorted[right]}`, detail: paired ? `Their total fits within limit ${limit}.` : `Their total exceeds limit ${limit}.`, activeLines: [6, 7], result: null }); if (paired) left += 1; right -= 1; boats += 1; push({ kind: "found", phase: "boat", title: paired ? "Send one shared boat" : "Send the heaviest passenger alone", detail: `Boats used: ${boats}.`, activeLines: paired ? [7, 8, 9, 10] : [9, 10], result: null }); } push({ kind: "done", phase: "done", title: `Return ${boats} boats`, detail: "Every passenger has now boarded exactly one boat.", activeLines: [11], result: boats }); return { frames }; }
