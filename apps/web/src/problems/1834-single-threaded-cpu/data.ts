export const title = "1834. Single-Threaded CPU";
export const examples = [
  { id: 1, label: "LeetCode 1", input: [[1, 2], [2, 4], [3, 2], [4, 1]], output: [0, 2, 3, 1] },
  { id: 2, label: "LeetCode 2", input: [[7, 10], [7, 12], [7, 5], [7, 4], [7, 2]], output: [4, 3, 2, 0, 1] },
];
export const defaultExample = examples[0]!;
export const codeLines = ["import heapq", "from typing import List", "", "class Solution:", "    def getOrder(self, tasks: List[List[int]]) -> List[int]:", "        new_tasks = []", "", "        # 给每个任务添加原始下标", "        for i, task in enumerate(tasks):", "            start = task[0]", "            process = task[1]", "", "            new_tasks.append((start, process, i))", "", "        # 按照任务进入时间排序", "        tasks = sorted(new_tasks)", "", "        heap = []", "        ans = []", "", "        time = 0", "        i = 0", "", "        while i < len(tasks) or heap:", "            # 当前没有可执行任务，直接跳到下一个任务的进入时间", "            if not heap and time < tasks[i][0]:", "                time = tasks[i][0]", "", "            # 把所有已经到达的任务加入最小堆", "            while i < len(tasks) and tasks[i][0] <= time:", "                start = tasks[i][0]", "                process = tasks[i][1]", "                index = tasks[i][2]", "", "                # 堆按照处理时间排序，处理时间相同则按照下标排序", "                heapq.heappush(heap, (process, index))", "", "                i += 1", "", "            # 取出当前处理时间最短的任务", "            process, index = heapq.heappop(heap)", "", "            # 执行任务", "            time += process", "            ans.append(index)", "", "        return ans"];
