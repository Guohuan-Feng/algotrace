import{describe,expect,test}from"vitest";import{createSearchBstDryRun}from"./dryRun";describe("Search in a Binary Search Tree dry run",()=>{test("chooses left or right from the BST ordering",()=>{const{frames}=createSearchBstDryRun([4,2,7,1,3],2);expect(frames[frames.length-1]?.result).toBe(2);expect(frames.some(f=>f.activeValue===2)).toBe(true);expect(frames[0]?.tree).toEqual([4,2,7,1,3]);});});
