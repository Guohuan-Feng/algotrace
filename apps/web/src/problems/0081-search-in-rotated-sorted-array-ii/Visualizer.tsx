import { useEffect, useMemo, useState } from "react";
import { ArrowLeft, Upload } from "lucide-react";
import { CodeTrace } from "../../shared/components/CodeTrace";
import { StepControls } from "../../shared/components/StepControls";
import type { VisualizerProps } from "../../shared/types";
import { codeLines, defaultExample, examples, title, type RotatedSearchIiInput } from "./data";
import { createRotatedSearchIiDryRun } from "./dryRun";

export default function SearchRotatedArrayIiVisualizer({ onBack }: VisualizerProps) {
  const [exampleId, setExampleId] = useState(defaultExample.id); const [input, setInput] = useState<RotatedSearchIiInput>(defaultExample.input); const [text, setText] = useState(JSON.stringify(defaultExample.input)); const [step, setStep] = useState(0); const [playing, setPlaying] = useState(false); const [error, setError] = useState("");
  const run = useMemo(() => createRotatedSearchIiDryRun(input.nums, input.target), [input]); const frame = run.frames[Math.min(step, run.frames.length - 1)]!; const selectedExample = examples.find((example) => example.id === exampleId);
  useEffect(() => { if (!playing || step >= run.frames.length - 1) { if (step >= run.frames.length - 1) setPlaying(false); return; } const timer = window.setTimeout(() => setStep((value) => value + 1), 580); return () => window.clearTimeout(timer); }, [playing, run.frames.length, step]);
  function load(example: (typeof examples)[number]) { setExampleId(example.id); setInput(example.input); setText(JSON.stringify(example.input)); setStep(0); setPlaying(false); setError(""); }
  function loadInput() { try { const candidate = JSON.parse(text) as RotatedSearchIiInput; if (!candidate || !Array.isArray(candidate.nums) || !candidate.nums.length || !candidate.nums.every(Number.isFinite) || !Number.isFinite(candidate.target)) throw new Error(); setInput(candidate); setExampleId(0); setStep(0); setPlaying(false); setError(""); } catch { setError('Use JSON such as {"nums":[2,5,6,0,0,1,2],"target":0}.'); } }
  return <main className="app-shell"><header className="topbar"><div><button className="back-link compact" onClick={onBack} type="button"><ArrowLeft size={16} />Catalog</button><p className="eyebrow">AlgoTrace dry run</p><h1>{title}</h1></div><div className="step-pill">Step {step + 1} / {run.frames.length}</div></header><section className="workspace"><aside className="board-panel"><div className="example-switcher" aria-label="LeetCode examples"><span>LeetCode examples</span><div>{examples.map((example) => <button className={example.id === exampleId ? "active" : ""} key={example.id} onClick={() => load(example)} type="button">{example.id}</button>)}</div></div><div className="input-grid"><label>input JSON<textarea aria-label="input JSON" value={text} onChange={(event) => setText(event.target.value)} /></label>{error ? <p className="error">{error}</p> : null}<button className="command load" onClick={loadInput} type="button"><Upload size={16} />Load input</button></div><div className="expected-output"><span>{selectedExample ? selectedExample.label + " output" : "Current result"}</span><code>{String(frame.result ?? selectedExample?.output ?? "pending")}</code></div></aside><section className="flow-panel binary-flow-panel"><div className="panel-heading"><h2>Rotated Binary Search</h2><span>target = {input.target}</span></div><div className="binary-stage"><div className="binary-array">{frame.nums.map((value, index) => { const inWindow = index >= frame.left && index <= frame.right; const inSorted = frame.sortedHalf === "left" ? index >= frame.left && index <= (frame.mid ?? -1) : frame.sortedHalf === "right" && index >= (frame.mid ?? Infinity) && index <= frame.right; const inRange = frame.targetRange ? index >= frame.targetRange[0] && index <= frame.targetRange[1] : false; return <div className={["binary-cell", inWindow ? "in-window" : "is-discarded", inSorted ? "is-sorted-half" : "", inRange ? "is-target-range" : "", index === frame.left ? "is-left" : "", index === frame.mid ? "is-mid" : "", index === frame.right ? "is-right" : "", frame.result && index === frame.mid ? "is-found" : ""].filter(Boolean).join(" ")} key={index}><strong>{value}</strong><span>{index}</span><div className="pointer-tags">{index === frame.left ? <em>L</em> : null}{index === frame.mid ? <em>M</em> : null}{index === frame.right ? <em>R</em> : null}</div></div>; })}</div></div></section><aside className="state-panel"><div className="state-sticky"><div className={"event-card " + frame.kind}><p className="eyebrow">{frame.phase}</p><h2>{frame.title}</h2><p>{frame.detail}</p></div><StepControls frameCount={run.frames.length} playing={playing} step={step} onPlayingChange={setPlaying} onStepChange={setStep} /></div><div className="state-block"><h3>binary-search state</h3><div className="token-list"><span>left = {frame.left}</span><span>mid = {frame.mid ?? "-"}</span><span>right = {frame.right}</span><span>sorted half = {frame.sortedHalf ?? "unknown"}</span><span>result = {String(frame.result ?? "pending")}</span></div></div><CodeTrace activeLines={frame.activeLines} codeLines={codeLines} /></aside></section></main>;
}
