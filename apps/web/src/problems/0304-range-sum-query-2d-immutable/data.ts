export type RangeSumQuery2dInput = { matrix: number[][]; row1: number; col1: number; row2: number; col2: number };
export const title = "304. Range Sum Query 2D - Immutable";
const matrix = [[3, 0, 1, 4, 2], [5, 6, 3, 2, 1], [1, 2, 0, 1, 5], [4, 1, 0, 1, 7], [1, 0, 3, 0, 5]];
export const examples = [{ id: 1, label: "LeetCode 1", input: { matrix, row1: 2, col1: 1, row2: 4, col2: 3 }, output: 8 }, { id: 2, label: "LeetCode 2", input: { matrix, row1: 1, col1: 1, row2: 2, col2: 2 }, output: 11 }, { id: 3, label: "LeetCode 3", input: { matrix, row1: 1, col1: 2, row2: 2, col2: 4 }, output: 12 }] satisfies Array<{ id: number; label: string; input: RangeSumQuery2dInput; output: number }>;
export const defaultExample = examples[0]!;
export const codeLines = ["class NumMatrix:", "    def __init__(self, matrix: List[List[int]]):", "        rows, cols = len(matrix), len(matrix[0])", "        self.prefix = [[0] * (cols + 1) for _ in range(rows + 1)]", "        for r in range(rows):", "            for c in range(cols):", "                self.prefix[r + 1][c + 1] = (", "                    matrix[r][c] + self.prefix[r][c + 1]", "                    + self.prefix[r + 1][c] - self.prefix[r][c]", "                )", "", "    def sumRegion(self, row1, col1, row2, col2):", "        return (self.prefix[row2 + 1][col2 + 1]", "                - self.prefix[row1][col2 + 1]", "                - self.prefix[row2 + 1][col1]", "                + self.prefix[row1][col1])"];
