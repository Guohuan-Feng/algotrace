import type {FrameKind}from"../../shared/types";type T={id:string;value:number;left:T|null;right:T|null};export type BalancedFrame={kind:FrameKind;phase:"start"|"enter"|"height"|"unbalanced"|"done";title:string;detail:string;activeLines:number[];activeId:string|null;heights:Record<string,number>;stack:string[];result:boolean|null};export function createBalancedTreeDryRun(v:Array<number|null>):{frames:BalancedFrame[]}{const root=build(v),frames:BalancedFrame[]=[],stack:string[]=[],heights:Record<string,number>={};const push=(f:Omit<BalancedFrame,"heights"|"stack">)=>frames.push({...f,heights:{...heights},stack:[...stack]});push({kind:"start",phase:"start",title:"Check balance bottom-up",detail:"Each subtree returns its height, or -1 as a failure signal.",activeLines:[15],activeId:root?.id??null,result:null});const h=dfs(root);push({kind:"done",phase:"done",title:h===-1?"Tree is not balanced":"Tree is balanced",detail:h===-1?"A node's child heights differ by more than one.":`Root height is ${h} and every node is balanced.`,activeLines:[15],activeId:null,result:h!==-1});return{frames};function dfs(n:T|null):number{if(!n)return 0;stack.push(`height(${n.value})`);push({kind:"visit",phase:"enter",title:`Measure node ${n.value}`,detail:"First obtain both child heights.",activeLines:[5],activeId:n.id,result:null});const l=dfs(n.left);if(l===-1){stack.pop();return-1;}const r=dfs(n.right);if(r===-1){stack.pop();return-1;}if(Math.abs(l-r)>1){push({kind:"prune",phase:"unbalanced",title:`Node ${n.value} is unbalanced`,detail:`|${l} - ${r}| > 1, so return -1 immediately.`,activeLines:[10,11],activeId:n.id,result:false});stack.pop();return-1;}const h=1+Math.max(l,r);heights[n.id]=h;push({kind:"backtrack",phase:"height",title:`Return height ${h} for ${n.value}`,detail:`1 + max(${l}, ${r}) = ${h}.`,activeLines:[13],activeId:n.id,result:null});stack.pop();return h;}}
function build(v:Array<number|null>):T|null{if(typeof v[0]!=="number")return null;const r:T={id:"node-0",value:v[0],left:null,right:null},q=[r];let i=1;while(q.length&&i<v.length){const n=q.shift()!,l=v[i],li=i++;if(typeof l==="number"){n.left={id:`node-${li}`,value:l,left:null,right:null};q.push(n.left);}if(i>=v.length)break;const x=v[i],xi=i++;if(typeof x==="number"){n.right={id:`node-${xi}`,value:x,left:null,right:null};q.push(n.right);}}return r;}
