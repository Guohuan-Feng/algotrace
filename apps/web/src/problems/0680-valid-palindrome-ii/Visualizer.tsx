import { ArrayTraceVisualizer } from "../../shared/components/ArrayTraceVisualizer"; import type { VisualizerProps } from "../../shared/types"; import { codeLines, examples, title, type ValidPalindromeIIInput } from "./data"; import { createValidPalindromeIIDryRun, type ValidPalindromeIIFrame } from "./dryRun";
export default function ValidPalindromeIIVisualizer(props: VisualizerProps) { return <ArrayTraceVisualizer<ValidPalindromeIIInput, ValidPalindromeIIFrame> {...props} title={title} examples={examples} codeLines={codeLines} heading="One-deletion palindrome check" inputLabel="input JSON" errorMessage='Use {"s":"abca"}.' parseInput={(value) => { const candidate = value as Partial<ValidPalindromeIIInput>; return typeof candidate?.s === "string" && candidate.s.length <= 32 ? { s: candidate.s } : null; }} createRun={(input) => createValidPalindromeIIDryRun(input.s)} renderState={(frame) => <div className="state-block"><h3>branch state</h3><div className="token-list"><span>left = {frame.left}</span><span>right = {frame.right}</span><span>skipped index = {frame.skipped ?? "-"}</span><span>skip left = {String(frame.firstBranch)}</span><span>skip right = {String(frame.secondBranch)}</span></div></div>} />; }
