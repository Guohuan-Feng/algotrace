export type ValidPalindromeIIInput = { s: string }; export type ValidPalindromeIIExample = { id: number; label: string; input: ValidPalindromeIIInput; output: boolean }; export const title = "680. Valid Palindrome II"; export const examples: ValidPalindromeIIExample[] = [{ id: 1, label: "LeetCode 1", input: { s: "aba" }, output: true }, { id: 2, label: "LeetCode 2", input: { s: "abca" }, output: true }, { id: 3, label: "LeetCode 3", input: { s: "abc" }, output: false }]; export const defaultExample = examples[0]!;
export const codeLines = ["class Solution:", "    def validPalindrome(self, s: str) -> bool:", "        def is_palindrome(left, right):", "            while left < right:", "                if s[left] != s[right]:", "                    return False", "                left += 1", "                right -= 1", "            return True", "        left, right = 0, len(s) - 1", "        while left < right:", "            if s[left] != s[right]:", "                return is_palindrome(left + 1, right) or is_palindrome(left, right - 1)", "            left += 1", "            right -= 1", "        return True"];
