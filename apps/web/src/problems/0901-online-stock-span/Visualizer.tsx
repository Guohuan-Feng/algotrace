import { useEffect, useMemo, useState } from "react"; import { ArrowLeft, Upload } from "lucide-react"; import { CodeTrace } from "../../shared/components/CodeTrace"; import { StepControls } from "../../shared/components/StepControls"; import type { VisualizerProps } from "../../shared/types"; import { codeLines, defaultExample, examples, title, type StockSpanInput } from "./data"; import { createStockSpanDryRun } from "./dryRun";
export default function StockSpanVisualizer({ onBack }: VisualizerProps) { const [input, setInput] = useState<StockSpanInput>(defaultExample.input); const [text, setText] = useState(JSON.stringify(defaultExample.input)); const [step, setStep] = useState(0); const [playing, setPlaying] = useState(false); const [error, setError] = useState(""); const run = useMemo(() => createStockSpanDryRun(input.prices), [input]); const frame = run.frames[Math.min(step, run.frames.length - 1)]!; useEffect(() => { if (!playing || step >= run.frames.length - 1) { if (step >= run.frames.length - 1) setPlaying(false); return; } const timer = window.setTimeout(() => setStep((value) => value + 1), 620); return () => window.clearTimeout(timer); }, [playing, run.frames.length, step]); function loadInput() { try { const candidate = JSON.parse(text) as StockSpanInput; if (!Array.isArray(candidate.prices) || !candidate.prices.length || !candidate.prices.every(Number.isFinite)) throw new Error(); setInput(candidate); setStep(0); setPlaying(false); setError(""); } catch { setError("Use JSON such as {\"prices\":[100,80,60,70,60,75,85]}."); } } return <main className="app-shell"><header className="topbar"><div><button className="back-link compact" onClick={onBack} type="button"><ArrowLeft size={16} />Catalog</button><p className="eyebrow">AlgoTrace dry run</p><h1>{title}</h1></div><div className="step-pill">Step {step +1} / {run.frames.length}</div></header><section className="workspace"><aside className="board-panel"><div className="input-grid"><label>input JSON<textarea aria-label="input JSON" value={text} onChange={(event) => setText(event.target.value)} /></label>{error ? <p className="error">{error}</p> : null}<button className="command load" onClick={loadInput} type="button"><Upload size={16} />Load prices</button></div><div className="expected-output"><span>LeetCode 1 output</span><code>{JSON.stringify(frame.result ?? examples[0].output)}</code></div></aside><section className="flow-panel product-flow-panel"><div className="panel-heading"><h2>Price-Span Monotonic Stack</h2><span>span = {frame.span ?? "-"}</span></div><div className="product-stage"><div className="product-numbers">{input.prices.map((price, index) => <div className={`product-number ${index === frame.index ? "is-current" : ""}`} key={index}><span>day {index + 1}</span><strong>{price}</strong></div>)}</div><div className="product-phase"><span>stack (price, span)</span><strong>{frame.stack.length ? frame.stack.map(([price, span]) => `(${price}, ${span})`).join(" ") : "[]"}</strong><p>{frame.detail}</p></div></div></section><aside className="state-panel"><div className="state-sticky"><div className={`event-card ${frame.kind}`}><p className="eyebrow">{frame.phase}</p><h2>{frame.title}</h2><p>{frame.detail}</p></div><StepControls frameCount={run.frames.length} playing={playing} step={step} onPlayingChange={setPlaying} onStepChange={setStep} /></div><CodeTrace activeLines={frame.activeLines} codeLines={codeLines} /></aside></section></main>; }
