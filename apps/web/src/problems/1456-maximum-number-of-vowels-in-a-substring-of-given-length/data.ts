export type MaxVowelsInput = { s: string; k: number }; export type MaxVowelsExample = { id: number; label: string; input: MaxVowelsInput; output: number }; export const title = "1456. Maximum Number of Vowels in a Substring of Given Length"; export const examples: MaxVowelsExample[] = [{ id: 1, label: "LeetCode 1", input: { s: "abciiidef", k: 3 }, output: 3 }, { id: 2, label: "LeetCode 2", input: { s: "aeiou", k: 2 }, output: 2 }]; export const defaultExample = examples[0]!;
export const codeLines = ["class Solution:", "    def maxVowels(self, s: str, k: int) -> int:", "        vowels = set('aeiou')", "        current = sum(char in vowels for char in s[:k])", "        answer = current", "        for right in range(k, len(s)):", "            current += s[right] in vowels", "            current -= s[right - k] in vowels", "            answer = max(answer, current)", "        return answer"];
