import { ArrayTraceVisualizer } from "../../shared/components/ArrayTraceVisualizer"; import type { VisualizerProps } from "../../shared/types"; import { codeLines, examples, title, type MaxVowelsInput } from "./data"; import { createMaxVowelsDryRun, type MaxVowelsFrame } from "./dryRun";
export default function MaxVowelsVisualizer(props: VisualizerProps) { return <ArrayTraceVisualizer<MaxVowelsInput, MaxVowelsFrame> {...props} title={title} examples={examples} codeLines={codeLines} heading="Fixed-size vowel window" inputLabel="input JSON" errorMessage='Use {"s":"abciiidef","k":3}.' parseInput={(value) => { const candidate = value as Partial<MaxVowelsInput>; return typeof candidate?.s === "string" && typeof candidate.k === "number" && Number.isInteger(candidate.k) && candidate.k > 0 && candidate.k <= candidate.s.length ? { s: candidate.s, k: candidate.k } : null; }} createRun={(input) => createMaxVowelsDryRun(input.s, input.k)} renderState={(frame) => <div className="state-block"><h3>vowel counts</h3><div className="token-list"><span>left = {frame.left}</span><span>right = {frame.right}</span><span>current = {frame.current}</span><span>maximum = {frame.maximum}</span></div></div>} />; }
