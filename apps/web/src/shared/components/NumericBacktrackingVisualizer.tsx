import { useEffect, useMemo, useState } from "react";
import { ArrowLeft, Upload } from "lucide-react";
import { CodeTrace } from "./CodeTrace";
import { StepControls } from "./StepControls";
import type { FrameKind, VisualizerProps } from "../types";

type Frame = { kind: FrameKind; title: string; detail: string; activeLines: number[]; nums?: number[]; candidates?: number[]; target?: number; total?: number; start: number; path: number[]; currentIndex: number | null; skippedIndex?: number | null; stack: string[]; results: number[][]; result: number[][] | null };
type Example<Input> = { id: number; label: string; input: Input; output: number[][] };
type Props<Input> = VisualizerProps & { title: string; valueName: string; examples: Example<Input>[]; defaultExample: Example<Input>; codeLines: string[]; inputToText: (input: Input) => string; parseInput: (text: string) => Input; createRun: (input: Input) => { frames: Frame[] }; values: (frame: Frame) => number[] };

export function NumericBacktrackingVisualizer<Input>({ onBack, title, valueName, examples, defaultExample, codeLines, inputToText, parseInput, createRun, values }: Props<Input>) {
  const [selectedExampleId, setSelectedExampleId] = useState(defaultExample.id); const [input, setInput] = useState<Input>(defaultExample.input); const [inputText, setInputText] = useState(inputToText(defaultExample.input)); const [step, setStep] = useState(0); const [playing, setPlaying] = useState(false); const [error, setError] = useState("");
  const selectedExample = examples.find((example) => example.id === selectedExampleId); const dryRun = useMemo(() => createRun(input), [createRun, input]); const frame = dryRun.frames[Math.min(step, dryRun.frames.length - 1)]!; const currentValues = values(frame);
  useEffect(() => { if (!playing || step >= dryRun.frames.length - 1) { if (step >= dryRun.frames.length - 1) setPlaying(false); return; } const timer = window.setTimeout(() => setStep((current) => current + 1), 540); return () => window.clearTimeout(timer); }, [dryRun.frames.length, playing, step]);
  const loadExample = (example: Example<Input>) => { setSelectedExampleId(example.id); setInput(example.input); setInputText(inputToText(example.input)); setStep(0); setPlaying(false); setError(""); };
  const loadInput = () => { try { setSelectedExampleId(0); setInput(parseInput(inputText)); setStep(0); setPlaying(false); setError(""); } catch { setError("Use the JSON format shown by the selected example."); } };
  return <main className="app-shell"><header className="topbar"><div><button className="back-link compact" onClick={onBack} type="button"><ArrowLeft size={16} />Catalog</button><p className="eyebrow">AlgoTrace dry run</p><h1>{title}</h1></div><div className="step-pill">Step {step + 1} / {dryRun.frames.length}</div></header><section className="workspace"><aside className="board-panel"><div className="example-switcher" aria-label="LeetCode examples"><span>LeetCode examples</span><div>{examples.map((example) => <button className={selectedExampleId === example.id ? "active" : ""} key={example.id} onClick={() => loadExample(example)} type="button">{example.id}</button>)}</div></div><div className="panel-heading"><h2>{valueName}</h2><span>backtracking</span></div><div className="input-grid"><label>input JSON<textarea value={inputText} onChange={(event) => setInputText(event.target.value)} /></label>{error ? <p className="error">{error}</p> : null}<button className="command load" onClick={loadInput} type="button"><Upload size={16} />Load input</button></div><div className="expected-output"><span>{selectedExample ? `${selectedExample.label} output` : "Current result"}</span><code>{JSON.stringify(selectedExample?.output ?? frame.result ?? "pending")}</code></div></aside><section className="flow-panel permutation-ii-flow-panel"><div className="panel-heading"><h2>Choice Frontier</h2><span>start = {frame.start}</span></div><div className="permutation-ii-stage"><div className="permutation-index-strip">{currentValues.map((value, index) => <div className={[frame.currentIndex === index ? "is-current" : "", frame.skippedIndex === index ? "is-skipped" : "", frame.path.includes(value) ? "is-used" : ""].filter(Boolean).join(" ")} key={`${value}-${index}`}><strong>{value}</strong><span>i = {index}</span><em>{frame.skippedIndex === index ? "skip duplicate" : "candidate"}</em></div>)}</div><div className="partition-path permutation-path"><h3>path</h3><div>{frame.path.length ? frame.path.map((value, index) => <span key={`${value}-${index}`}>{value}</span>) : <em>[]</em>}</div></div><div className="permutation-results"><h3>res</h3><div>{frame.results.length ? frame.results.map((result, index) => <span key={`${result.join("-")}-${index}`}>[{result.join(", ")}]</span>) : <em>none yet</em>}</div></div></div></section><aside className="state-panel"><div className="state-sticky"><div className={`event-card ${frame.kind}`}><p className="eyebrow">{frame.kind}</p><h2>{frame.title}</h2><p>{frame.detail}</p></div><StepControls frameCount={dryRun.frames.length} playing={playing} step={step} onPlayingChange={setPlaying} onStepChange={setStep} /></div><div className="state-block"><h3>current state</h3><div className="token-list"><span>start = {frame.start}</span>{frame.target !== undefined ? <span>target = {frame.target}</span> : null}{frame.total !== undefined ? <span>total = {frame.total}</span> : null}<span>path = [{frame.path.join(", ")}]</span></div></div><div className="state-block"><h3>call stack</h3><div className="token-list">{frame.stack.length ? frame.stack.map((item, index) => <span key={`${item}-${index}`}>{item}</span>) : <em>empty</em>}</div></div><CodeTrace activeLines={frame.activeLines} codeLines={codeLines} /></aside></section></main>;
}
